# Copy this file to values-secrets.yaml and fill in real values.
# values-secrets.yaml is gitignored - never commit real credentials.
secrets:
  postgresUser: "vocab"
  postgresPassword: "vocab"
  postgresDb: "vocab"
